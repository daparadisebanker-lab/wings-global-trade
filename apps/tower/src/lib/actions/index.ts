// src/lib/actions — server-action mutation layer (auth → Zod → RLS query).
// See ./result.ts for the mutation law and the ActionResult contract.
export * from './result'
export { whoami } from './session'
