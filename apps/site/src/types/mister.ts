// src/types/mister.ts
// The Mister v2 type/data contract moved to @wings/mister (M3c). Re-exported here
// so every importer of `@/types/mister` (11 server files + all client components)
// is unchanged. See MIGRATION_DECISIONS D-03/D-11.
export * from '@wings/mister'
